<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sr.dao.FileDao">

    <!--根据用户id查询当前用户的文件信息-->
    <select id="findByUserId" parameterType="Integer" resultType="com.sr.domain.FileDo">
        select  id,oldFileName,newFileName,ext,path,size,type,isImg,downcounts,uploadTime,userId
        from mall_file
        where userId=#{id}
    </select>


    <!--保存文件信息-->
    <insert id="save" parameterType="com.sr.domain.FileDo" useGeneratedKeys="true" keyProperty="id">
        insert into mall_file
        values (#{id},#{oldFileName},#{newFileName},
                #{ext},	#{path},#{size},#{type},
                #{isImg},#{downcounts},	#{uploadTime},#{userId})
    </insert>


    <!--根据id获取文件信息-->
    <select id="findById" parameterType="String" resultType="com.sr.domain.FileDo">
        select id,oldFileName,newFileName,ext,path,size,type,isImg,downcounts,uploadTime,userId
        from mall_file
        where id = #{id}
    </select>

    <!--更新下载次数-->
    <update id="update" parameterType="com.sr.domain.FileDo" >
        update mall_file set downcounts=#{downcounts} where id=#{id}
    </update>

    <!--根据id删除记录-->
    <delete id="delete" parameterType="String">
        delete from mall_file where id=#{id}
    </delete>

</mapper>